# /log-session

Create a session log entry in `logs/sessions/`.

Usage: `/log-session <slug> <summary>`

This command writes a markdown session log file to `logs/sessions/YYYY-MM-DD-<slug>.md` with a summary of what was done this session, decisions made, and what to hand off to the next session.
